<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="/my.css">
    <title>{{title}}</title>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Джурів</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                {{#if isHome}}
                    <a class="nav-link active" href="/">Головна</a>
                {{else}}
                    <a class="nav-link" href="/">Головна</a>
                {{/if}}
            </li>

            {{#if isAdmin}}

                <li class="nav-item dropdown">
                    {{#if isCatalog}}
                        <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Каталог
                        </a>
                    {{else}}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Каталог
                        </a>
                    {{/if}}
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/medicine">Ліки</a>
                    <a class="dropdown-item" href="/employees">Співробітники</a>
                    <a class="dropdown-item" href="/patients">Пацієнти</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/medicine?isActive=true">Списані ліки</a>
                </li>
                <li class="nav-item dropdown">
                    {{#if isAdd}}
                        <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Додати
                        </a>
                    {{else}}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Додати
                        </a>
                    {{/if}}
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/addMedicine">Ліки</a>
                    <a class="dropdown-item" href="/addEmployee">Співробітники</a>
                    <a class="dropdown-item" href="/addPatient">Пацієнти</a>
                </li>
                <li class="nav-item">
                    {{#if isIncoming}}
                        <a class="nav-link active" href="/addIncoming">Прихід</a>
                    {{else}}
                        <a class="nav-link" href="/addIncoming">Прихід</a>
                    {{/if}}
                </li>
                <li class="nav-item">
                    {{#if isConsumption}}
                        <a class="nav-link active" href="/addConsumption">Розхід</a>
                    {{else}}
                        <a class="nav-link" href="/addConsumption">Розхід</a>
                    {{/if}}
                </li>
                <li class="nav-item dropdown">
                    {{#if isReport}}
                        <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Звіт
                        </a>
                    {{else}}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Звіт
                        </a>
                    {{/if}}
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/inReport">Прихід</a>
                    <a class="dropdown-item" href="/outReport">Розхід</a>
                    </div>
                </li>
                <li class="nav-item"><a class="nav-link" href="/auth/logout">Вихід</a></li>

            {{else}}
                <li class="nav-item">
                    {{#if isLogin}}
                        <a class="nav-link active" href="/auth/login">login</a>
                    {{else}}
                        <a class="nav-link" href="/auth/login">login</a>
                    {{/if}}
                </li>
            {{/if}}

            
        </ul>
        <form class="form-inline my-2 my-lg-0"> 
            <input id="searchInput" class="form-control mr-sm-2" type="search" placeholder="Назва препарату" aria-label="Search">
            <button id="searchButton" class="btn btn-outline-success my-2 my-sm-0" type="submit">Пошук</button>
        </form>
    </div>
    </nav>