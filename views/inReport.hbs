<h1>Звіт по приходу з : {{from}} по {{to}}</h1>

{{#if incomings.length}}
    <table class="table">
        <thead class="thead-dark">
            <tr>
            <th scope="col">#</th>
            <th scope="col">Назва</th>
            <th scope="col">Кількість</th>
            </tr>
        </thead>
        {{#each incomings}}
            <tbody>
                <tr>
                <th scope="row">{{@index}}</th>
                <td>{{Medicine.title}}</td>
                <td>{{quantity}}</td>
                </tr>
            </tbody>
        {{/each}}

                
    </table>
{{else}}
    <h1>Не має поступлень за цей період</h1>
{{/if}}